syntax = "proto3";

package audio;

service AudioMeta {
    rpc GetMeta (AudioMetaReq) returns (AudioMetaRes) {}
}

message AudioMetaReq {
    string filename = 1;
}

message AudioMetaRes {
    uint32 bit_rate = 1;
    uint32 channels = 2;
    uint32 sample_frames = 3;
    uint32 sample_rate = 4;
}